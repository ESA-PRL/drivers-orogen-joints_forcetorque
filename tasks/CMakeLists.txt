# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(joints_forcetorqueTaskLib)
ADD_LIBRARY(${JOINTS_FORCETORQUE_TASKLIB_NAME} SHARED 
    ${JOINTS_FORCETORQUE_TASKLIB_SOURCES})
add_dependencies(${JOINTS_FORCETORQUE_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${JOINTS_FORCETORQUE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${JOINTS_FORCETORQUE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${JOINTS_FORCETORQUE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${JOINTS_FORCETORQUE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${JOINTS_FORCETORQUE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${JOINTS_FORCETORQUE_TASKLIB_HEADERS}
    DESTINATION include/orocos/joints_forcetorque)

